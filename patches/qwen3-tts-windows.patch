diff --git a/src/audio_tokenizer_encoder.cpp b/src/audio_tokenizer_encoder.cpp
index d264673..4f76c63 100644
--- a/src/audio_tokenizer_encoder.cpp
+++ b/src/audio_tokenizer_encoder.cpp
@@ -2,7 +2,13 @@
 #include "gguf_loader.h"
 #include "ggml-cpu.h"
 
+#ifdef _MSC_VER
+#define _USE_MATH_DEFINES
+#endif
 #include <cmath>
+#ifndef M_PI
+#define M_PI 3.14159265358979323846
+#endif
 #include <cstring>
 #include <algorithm>
 #include <numeric>
diff --git a/src/qwen3_tts.cpp b/src/qwen3_tts.cpp
index c2f3434..4ac36bb 100644
--- a/src/qwen3_tts.cpp
+++ b/src/qwen3_tts.cpp
@@ -11,6 +11,12 @@
 
 #ifdef __APPLE__
 #include <mach/mach.h>
+#elif defined(_WIN32)
+#define WIN32_LEAN_AND_MEAN
+#define NOMINMAX
+#include <windows.h>
+#include <psapi.h>
+#pragma comment(lib, "psapi.lib")
 #else
 #include <sys/resource.h>
 #endif
@@ -46,6 +52,15 @@ static bool get_process_memory_snapshot(process_memory_snapshot & out) {
         out.phys_footprint_bytes = out.rss_bytes;
     }
     return true;
+#elif defined(_WIN32)
+    PROCESS_MEMORY_COUNTERS pmc{};
+    pmc.cb = sizeof(pmc);
+    if (!GetProcessMemoryInfo(GetCurrentProcess(), &pmc, sizeof(pmc))) {
+        return false;
+    }
+    out.rss_bytes = (uint64_t) pmc.WorkingSetSize;
+    out.phys_footprint_bytes = out.rss_bytes;
+    return true;
 #else
     struct rusage usage = {};
     if (getrusage(RUSAGE_SELF, &usage) != 0) {
