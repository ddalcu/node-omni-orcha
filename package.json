{
  "name": "@agent-orcha/node-omni-orcha",
  "version": "0.1.0",
  "description": "Unified native Node.js bindings — single omni.node for LLM, TTS, and Image/Video generation",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "engines": {
    "node": ">=25.0.0"
  },
  "scripts": {
    "build": "cmake-js compile --parallel --CDGPU_BACKEND=auto",
    "build:metal": "cmake-js compile --parallel --CDGPU_BACKEND=metal",
    "build:cuda": "cmake-js compile --parallel --CDGPU_BACKEND=cuda",
    "build:cuda:release": "cmake-js compile --parallel --CDGPU_BACKEND=cuda --CDCUDA_RELEASE=ON",
    "build:cpu": "cmake-js compile --parallel --CDGPU_BACKEND=cpu",
    "rebuild": "cmake-js rebuild",
    "clean": "cmake-js clean",
    "test": "node --test test/*.test.ts",
    "test:llm": "node --test test/llm.test.ts",
    "test:unit": "node --test test/gguf-reader.test.ts test/binding-loader.test.ts",
    "test:samuel": "node scripts/samuel-jackson-test.ts",
    "build:ts": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build:ts",
    "serve": "node server.ts"
  },
  "dependencies": {
    "node-addon-api": "^8.3.1"
  },
  "optionalDependencies": {
    "@agent-orcha/node-omni-orcha-darwin-arm64": "0.1.0",
    "@agent-orcha/node-omni-orcha-linux-x64": "0.1.0",
    "@agent-orcha/node-omni-orcha-linux-x64-cuda": "0.1.0",
    "@agent-orcha/node-omni-orcha-linux-arm64": "0.1.0",
    "@agent-orcha/node-omni-orcha-win32-x64": "0.1.0",
    "@agent-orcha/node-omni-orcha-win32-x64-cuda": "0.1.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "cmake-js": "^7.3.0",
    "typescript": "^5.8.0"
  },
  "files": [
    "dist/",
    "src/"
  ],
  "keywords": [
    "llm", "tts", "llama", "stable-diffusion", "flux",
    "qwen", "qwen3-tts", "voice-cloning", "text-to-speech",
    "image-generation", "ggml", "gguf", "native", "n-api", "metal", "cuda"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ddalcu/node-omni-orcha"
  }
}
